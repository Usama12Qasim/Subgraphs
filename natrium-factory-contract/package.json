{
  "name": "natrium-factory-contract",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ natrium-factory-contract",
    "create-local": "graph create --node http://localhost:8020/ natrium-factory-contract",
    "remove-local": "graph remove --node http://localhost:8020/ natrium-factory-contract",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 natrium-factory-contract",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.93.1",
    "@graphprotocol/graph-ts": "0.36.0"
  },
  "devDependencies": { "matchstick-as": "0.6.0" }
}
